import config from '@/common/config.js'

const state = {
  domain: config.domain + '/prod-api'
}

const mutations = {
}

const actions = {
}

export default {
  namespaced: true,
  state,
  mutations,
  actions
}
